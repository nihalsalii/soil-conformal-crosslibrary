# Analysis environment for "How reliable are prediction intervals from
# global soil spectral libraries? A cross-library conformal benchmark"
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# Required only by asama06_cnn.py (the 1D-CNN model family).
# Install the CPU build with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
